// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"gitee.com/meinongyihe/travel-rpc/ent/exchangerate"
)

// ExchangeRate is the model entity for the ExchangeRate schema.
type ExchangeRate struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 基准币种
	BaseCurrency string `json:"base_currency,omitempty"`
	// 目标币种
	TargetCurrency string `json:"target_currency,omitempty"`
	// 实际汇率×1e6
	RateMicro int64 `json:"rate_micro,omitempty"`
	// 来源：MANUAL/API
	Source string `json:"source,omitempty"`
	// 生效时间戳(unix秒)
	EffectiveAt int64 `json:"effective_at,omitempty"`
	// 状态：ACTIVE/INACTIVE
	Status       string `json:"status,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ExchangeRate) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case exchangerate.FieldID, exchangerate.FieldRateMicro, exchangerate.FieldEffectiveAt:
			values[i] = new(sql.NullInt64)
		case exchangerate.FieldBaseCurrency, exchangerate.FieldTargetCurrency, exchangerate.FieldSource, exchangerate.FieldStatus:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ExchangeRate fields.
func (_m *ExchangeRate) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case exchangerate.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case exchangerate.FieldBaseCurrency:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field base_currency", values[i])
			} else if value.Valid {
				_m.BaseCurrency = value.String
			}
		case exchangerate.FieldTargetCurrency:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field target_currency", values[i])
			} else if value.Valid {
				_m.TargetCurrency = value.String
			}
		case exchangerate.FieldRateMicro:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field rate_micro", values[i])
			} else if value.Valid {
				_m.RateMicro = value.Int64
			}
		case exchangerate.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				_m.Source = value.String
			}
		case exchangerate.FieldEffectiveAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field effective_at", values[i])
			} else if value.Valid {
				_m.EffectiveAt = value.Int64
			}
		case exchangerate.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ExchangeRate.
// This includes values selected through modifiers, order, etc.
func (_m *ExchangeRate) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ExchangeRate.
// Note that you need to call ExchangeRate.Unwrap() before calling this method if this ExchangeRate
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ExchangeRate) Update() *ExchangeRateUpdateOne {
	return NewExchangeRateClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ExchangeRate entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ExchangeRate) Unwrap() *ExchangeRate {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ExchangeRate is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ExchangeRate) String() string {
	var builder strings.Builder
	builder.WriteString("ExchangeRate(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("base_currency=")
	builder.WriteString(_m.BaseCurrency)
	builder.WriteString(", ")
	builder.WriteString("target_currency=")
	builder.WriteString(_m.TargetCurrency)
	builder.WriteString(", ")
	builder.WriteString("rate_micro=")
	builder.WriteString(fmt.Sprintf("%v", _m.RateMicro))
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(_m.Source)
	builder.WriteString(", ")
	builder.WriteString("effective_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.EffectiveAt))
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteByte(')')
	return builder.String()
}

// ExchangeRates is a parsable slice of ExchangeRate.
type ExchangeRates []*ExchangeRate
