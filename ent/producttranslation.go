// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"gitee.com/meinongyihe/travel-rpc/ent/producttranslation"
)

// ProductTranslation is the model entity for the ProductTranslation schema.
type ProductTranslation struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// 关联产品 ID
	ProductID int64 `json:"product_id,omitempty"`
	// 目标语言：zh-CN/en-US/ar
	Locale string `json:"locale,omitempty"`
	// 标题译文
	Title string `json:"title,omitempty"`
	// 描述译文
	Description string `json:"description,omitempty"`
	// 亮点译文(JSON)
	Highlights string `json:"highlights,omitempty"`
	// 图文介绍译文(富文本)
	RichContent string `json:"rich_content,omitempty"`
	// 预订须知译文
	BookingNotice string `json:"booking_notice,omitempty"`
	// 来源：MACHINE/MANUAL
	Source string `json:"source,omitempty"`
	// 状态：PENDING/DONE/FAILED
	Status string `json:"status,omitempty"`
	// 最近翻译时间戳(unix秒)
	UpdatedAt    int64 `json:"updated_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*ProductTranslation) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case producttranslation.FieldID, producttranslation.FieldProductID, producttranslation.FieldUpdatedAt:
			values[i] = new(sql.NullInt64)
		case producttranslation.FieldLocale, producttranslation.FieldTitle, producttranslation.FieldDescription, producttranslation.FieldHighlights, producttranslation.FieldRichContent, producttranslation.FieldBookingNotice, producttranslation.FieldSource, producttranslation.FieldStatus:
			values[i] = new(sql.NullString)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the ProductTranslation fields.
func (_m *ProductTranslation) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case producttranslation.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			_m.ID = int(value.Int64)
		case producttranslation.FieldProductID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field product_id", values[i])
			} else if value.Valid {
				_m.ProductID = value.Int64
			}
		case producttranslation.FieldLocale:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field locale", values[i])
			} else if value.Valid {
				_m.Locale = value.String
			}
		case producttranslation.FieldTitle:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field title", values[i])
			} else if value.Valid {
				_m.Title = value.String
			}
		case producttranslation.FieldDescription:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field description", values[i])
			} else if value.Valid {
				_m.Description = value.String
			}
		case producttranslation.FieldHighlights:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field highlights", values[i])
			} else if value.Valid {
				_m.Highlights = value.String
			}
		case producttranslation.FieldRichContent:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field rich_content", values[i])
			} else if value.Valid {
				_m.RichContent = value.String
			}
		case producttranslation.FieldBookingNotice:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field booking_notice", values[i])
			} else if value.Valid {
				_m.BookingNotice = value.String
			}
		case producttranslation.FieldSource:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field source", values[i])
			} else if value.Valid {
				_m.Source = value.String
			}
		case producttranslation.FieldStatus:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field status", values[i])
			} else if value.Valid {
				_m.Status = value.String
			}
		case producttranslation.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Int64
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the ProductTranslation.
// This includes values selected through modifiers, order, etc.
func (_m *ProductTranslation) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// Update returns a builder for updating this ProductTranslation.
// Note that you need to call ProductTranslation.Unwrap() before calling this method if this ProductTranslation
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *ProductTranslation) Update() *ProductTranslationUpdateOne {
	return NewProductTranslationClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the ProductTranslation entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *ProductTranslation) Unwrap() *ProductTranslation {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: ProductTranslation is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *ProductTranslation) String() string {
	var builder strings.Builder
	builder.WriteString("ProductTranslation(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("product_id=")
	builder.WriteString(fmt.Sprintf("%v", _m.ProductID))
	builder.WriteString(", ")
	builder.WriteString("locale=")
	builder.WriteString(_m.Locale)
	builder.WriteString(", ")
	builder.WriteString("title=")
	builder.WriteString(_m.Title)
	builder.WriteString(", ")
	builder.WriteString("description=")
	builder.WriteString(_m.Description)
	builder.WriteString(", ")
	builder.WriteString("highlights=")
	builder.WriteString(_m.Highlights)
	builder.WriteString(", ")
	builder.WriteString("rich_content=")
	builder.WriteString(_m.RichContent)
	builder.WriteString(", ")
	builder.WriteString("booking_notice=")
	builder.WriteString(_m.BookingNotice)
	builder.WriteString(", ")
	builder.WriteString("source=")
	builder.WriteString(_m.Source)
	builder.WriteString(", ")
	builder.WriteString("status=")
	builder.WriteString(_m.Status)
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(fmt.Sprintf("%v", _m.UpdatedAt))
	builder.WriteByte(')')
	return builder.String()
}

// ProductTranslations is a parsable slice of ProductTranslation.
type ProductTranslations []*ProductTranslation
